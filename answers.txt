What is a remote repository?

A remote repository is the code all collaborators share and build off of

What is the difference between a merge and a rebase?

A merge adds two histories together while a rebase changes the original version of a branch